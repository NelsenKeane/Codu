import 'package:flutter/material.dart';

class AppColors {
  // Brand colors from mascot & logo
  static const Color purple = Color(0xFF7F50CA);      // Codu purple ('o' in logo, screen background)
  static const Color yellow = Color(0xFFFFA800);      // Codu yellow ('u' in logo, primary 3D button)
  static const Color yellowShadow = Color(0xFFD97E00); // Shadow for primary yellow button
  static const Color yellowBackButtonShadow = Color(0xFFC78100); // Shadow for yellow back button
  static const Color green = Color(0xFF46B830);       // Codu green (mascot base, 'c' in logo, active fields)
  static const Color greenShadow = Color(0xFF339320);  // Shadow for green button
  static const Color blue = Color(0xFF3B69CF);        // Codu blue ('d' in logo, links)
  static const Color lightGreen = Color(0xFF95FF7A);   // Codu mascot neon green (eyes, text highlight)
  
  // UI & Scaffold Colors
  static const Color background = Colors.white;
  static const Color inputBackground = Color(0xFFF7F8FA);
  static const Color borderGrey = Color(0xFFE5E5E5);
  static const Color borderActive = Color(0xFF46B830); // Green when active
  static const Color skyBlue = Color(0xFF56CCF2);      // Sky blue header background
  static const Color cardBackground = Color(0xFFE8F4FD); // Light blue card container background
  static const Color inputBackgroundLight = Color(0xFFEAECEF); // Lighter input field background
  static const Color tabInactiveBg = Color(0xFFB0B2C3); // Inactive tab background (Register)
  
  // Text Colors
  static const Color textDark = Color(0xFF2B2D42);     // High legibility charcoal
  static const Color textGrey = Color(0xFF8F9BB3);     // Subtle grey
  static const Color textLight = Colors.white;
  
  // Social button colors
  static const Color facebookBlue = Color(0xFF1877F2);
  static const Color googleRed = Color(0xFFEA4335);
}
